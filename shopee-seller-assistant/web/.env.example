# URL of the running REST API backend (Sprint A)
BACKEND_URL=http://localhost:4000
# Provisioned at server boot for the single-user MVP
STORE_NAME=Toko Saya
SELLER_TIER=REGULAR

# Optional: publish a Google Sheet (File > Share > Publish to web > CSV) with
# columns main,label,pct to override the built-in Shopee admin-fee category list
# live (no redeploy). Leave unset to use the built-in table.
NEXT_PUBLIC_FEE_SHEET_CSV=
